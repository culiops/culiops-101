{
  "name": "hello-fargate",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny API used to demo Docker -> ECR -> ECS Fargate",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "4.21.2"
  }
}
